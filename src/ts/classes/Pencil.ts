/// <reference path="Tool.ts" />
class Pencil extends Tool {

	// This needs to set an event listener on the canvas,
	// so it probably needs to communicate with the Container?
	protected enableDrawListener(): void {

	}

	protected disableDrawListener(): void {

	}

	
}